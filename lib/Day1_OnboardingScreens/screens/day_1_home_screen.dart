import 'package:flutter/material.dart';

class Day1HomeScreen extends StatelessWidget {
  const Day1HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Day 1'),
        centerTitle: true,
      ),
    );
  }
}
